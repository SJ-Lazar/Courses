public class Constructors
{
    public Constructors()
    {
        //Default constructor
    }
    public Constructors(string message)
    {
        //Parameterised constructor
    }

    //Constructors and inheritance

    //Base class constructor is always called first
    //If no base class constructor is specified, the default constructor is called
    //If a base class constructor is specified, the default constructor is not called
    //base class constructors are not inherited
}

