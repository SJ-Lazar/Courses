public class HostedServices 
{
    //What is a hosted service?
    //A hosted service is a class with background task logic that
    //implements the IHostedService interface.

    //What is the IHostedService interface?
    //The IHostedService interface is used to notify your application

    //when the app host has started and stopped.
    //The IHostedService interface has two methods:
    //StartAsync and StopAsync.

}
